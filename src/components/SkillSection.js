import SkillCard from "./SkillCard";

const SkillSection = () => {
  return (
    <div className="skill-section bg-secondary">
      <h3 className="text-center ">Skills</h3>
      <SkillCard />
    </div>
  );
};

export default SkillSection;
